<!DOCTYPE html>
<html>
  <head>
    <title>Paynow</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="icon" href="favicon.ico" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-light d-flex justify-content-around" style="height:12vh;">
      <h1 style="text-align:center;" class="text-center">Bem vindo ao PayNow</h1>

      <div class="">
          <button class="btn btn-primary me-md-2" type="button">Entrar</button>
          <button class="btn btn-success" type="button">Crie sua conta</button>
      </div>
    </nav>

    <article class="container text-center" style="height:83vh">
      <% if notice %>
        <p><%= notice %></p>
      <% end %>
      <% if alert %>
        <p><%= alert %></p>
      <% end %>
      <%= yield %>
    </article>

    <footer class="d-grid gap-2 d-md-flex justify-content-md-end" style="height:5vh">
      <%= link_to 'Área do admin', new_admin_session_path, method: :get, class: "btn btn-outline-primary", style: "" %>
    </footer>
  </body>
</html>
